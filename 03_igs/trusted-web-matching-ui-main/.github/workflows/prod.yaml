on:
  workflow_dispatch:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+*'

name: trusted-web/prod

jobs:
  call_workflow:
    uses: Institution-for-a-Global-Society/trusted-web-infra/.github/workflows/prod-ui-matching.yaml@main
    secrets: inherit
