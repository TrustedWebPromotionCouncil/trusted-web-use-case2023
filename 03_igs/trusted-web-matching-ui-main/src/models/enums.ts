export namespace Enums {
  export enum Gender {
    Male = 'Male',
    Female = 'Female',
    Other = 'Other',
  }

  export enum DialogIndex {
    Detail = 1,
    Offer = 2,
  }

  export enum AbilityType {
    Balanced = 1,
    HardSkill = 2,
    SoftSkill = 3,
  }
}
